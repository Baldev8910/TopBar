<Window x:Class="TopBar.RemindersListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Reminders"
        Height="500"
        Width="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
           Text="Reminders"
           FontFamily="Segoe UI"
           FontSize="24"
           FontWeight="SemiBold"
           Foreground="White"
           Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
           x:Name="SubtitleText"
           FontFamily="Segoe UI"
           FontSize="13"
           Foreground="White"
           Opacity="0.6"
           Margin="0,0,0,16"/>

        <!-- Reminders List -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="RemindersList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1AFFFFFF"
                                CornerRadius="8"
                                Padding="16,12"
                                Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Reminder Info -->
                                <StackPanel Grid.Column="0"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}"
                                               FontFamily="Segoe UI"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding DateTimeDisplay}"
                                               FontFamily="Segoe UI"
                                               FontSize="12"
                                               Foreground="#AAAAAA"/>
                                    <TextBlock Text="{Binding RepeatDisplay}"
                                               FontFamily="Segoe UI"
                                               FontSize="12"
                                               Foreground="#0078D4"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Content="✏️"
                                            FontSize="14"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Padding="8,4"
                                            ToolTip="Edit"
                                            Tag="{Binding Id}"
                                            Click="Edit_Click"/>
                                    <Button Content="🗑️"
                                            FontSize="14"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Padding="8,4"
                                            ToolTip="Delete"
                                            Tag="{Binding Id}"
                                            Click="Delete_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel x:Name="EmptyState"
                    Grid.Row="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="Collapsed">
            <TextBlock Text="🔔"
                       FontSize="40"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,12"/>
            <TextBlock Text="No reminders yet"
                       FontFamily="Segoe UI"
                       FontSize="15"
                       Foreground="#AAAAAA"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Right-click the bar to add one"
                       FontFamily="Segoe UI"
                       FontSize="12"
                       Foreground="#666666"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>
        </StackPanel>

    </Grid>
</Window>